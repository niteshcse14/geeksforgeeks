#include <bits/stdc++.h>
using namespace std;
int arr[50001];
int main()
{
	int T, N;
	while(T--)
	{
		cin >> N;
		for(int i = 0; i < N; i++)
		{
			cin >> arr[i];
		}
		unordered_set<int> k;
		//
	}
	return 0;
}